import * as React from 'react';
import { Button } from '@adobe/react-spectrum';
import '../scss/splash.scss';

export class Splash extends React.Component<Record<string, unknown>> {
  public render() {
    return (
      <main className="splash-page">
        Spalsh
      </main>
    )
  }
}
